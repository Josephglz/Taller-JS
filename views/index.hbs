<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto Carpetas</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-slate-800">
    {{> menu}}
    {{#if status}}
        <div class="flex flex-wrap ml-32 mr-32 mb-5 justify-start select-none">
            {{#each data}}
                <a href="/detalles/{{id}}">
                    <div  class="flex flex-col m-2 p-3 w-[290px] bg-slate-200 rounded-md shadow-sm hover:animate-pulse">
                        <p class="w-full truncate">ID: {{id}}</p>
                        <p class="w-full truncate">Nombre: {{asignatura}}</p>
                        <p class="w-full truncate">Docente: {{docente}}</p>
                        <p class="w-full truncate">Duración: {{duracion}}</p>
                        <p class="w-full truncate">Horario: {{horario}}</p>
                    </div>
                </a>
            {{/each}}
        </div>
    {{else}}
        <h2 class="text-xl text-slate-200 font-medium">No hay datos</h2>
    {{/if}}
</body>
</html>