<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto Carpetas</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-slate-800">
    {{> menu}}

    <div class="ml-40 mr-40 mb-10 bg-slate-200 p-5 rounded-md shadow-md">
        <div class="flex flex-row items-center">
            <h2 class="w-[80%] text-4xl font-Lato font-bold text-slate-700">{{data.asignatura}}</h2>
            <h2 class="w-[20%] text-xl text-right text-slate-700 font-medium">{{fecha data.fecha}}</h2>
        </div>
        <h2 class="text-base text-slate-700">Docente: {{data.docente}}</h2>
        <h2 class="text-base text-slate-700">Horario: {{data.horario}}</h2>

        <div class="flex flex-wrap mt-12 justify-start">
            {{#if img}}
                {{#each imagenes}}
                <div class="w-[357px] h-80 m-2 rounded-lg shadow-md bg-gray-800 border-gray-700">
                    <img class="rounded-t-lg mx-auto"  src="/public/images/{{nombre}}" alt="" />
                    <div class="p-5">
                        <span class="bg-green-100 text-green-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded dark:bg-green-200 dark:text-green-900">Nota</span>
                        <p class="mb-3 mt-3 font-normal text-gray-700 dark:text-gray-400">{{descripcion}}</p>
                    </div>
                </div>
                {{/each}}
            {{else}}
                <h2 class="text-xl text-slate-700 font-medium">No hay imagenes</h2>
            {{/if}}
        </div>
    </div>
    
    
    
        <img src="" alt="">
    
</body>
</html>